String[] arr_in = {"secret", "3", "2", "checkout", "The"};
        int n = arr_in.length;
        int count = 0;
        

        for (int i = 0; i < n; i++) {
            if (arr_in[i].length() <= 3) {
                count = count + 1;
            }
        }
        System.out.println(count);
